# Render static production pages in V0

Showrunner V0 will render production pages as static HTML files generated from JSON production state. This keeps review and handoff artifacts portable and simple, while leaving a live local web app for later if interactive approvals or real-time review become necessary.
