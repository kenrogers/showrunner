# Use OpenRouter Agent SDK as the V0 framework

Showrunner V0 will use `@openrouter/agent` as the agent framework and OpenRouter as the model and generation engine. Flue remains a useful future option for hosted, headless, durable runtime deployments, but V0 needs a TUI-centered local harness with direct OpenRouter model, video, image, speech, and audio routing.
