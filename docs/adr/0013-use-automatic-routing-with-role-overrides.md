# Use automatic routing with role overrides

Showrunner will choose models automatically by default through routing policies such as best quality, balanced, and budget-aware, while allowing explicit overrides per role and modality. This preserves OpenRouter model choice as a power feature without requiring users to manually manage every role, reviewer, video model, TTS model, or music model.
