# Use JSON state and HTML human surfaces

Showrunner will keep structured JSON as the source of truth for production state, while rendering human-readable HTML pages for review, steering, and handoff. Markdown can be used for developer docs, but production-facing progress, reviews, and summaries should be dynamic HTML so they can present media, status, costs, and decisions more clearly.
